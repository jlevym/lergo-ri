lergo-build:
  build:
    image: lergo-build
    dockerfile: Dockerfile
    volumes:
      - ./tmp/artifacts:/artifacts
  encrypted_env_file: env.encrypted
lergo-test:
  build:
    image: lergo-build
    dockerfile: Dockerfile
    volumes_from:
      - lergo-build
  encrypted_env_file: env.encrypted  
